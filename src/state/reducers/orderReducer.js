const initialState= {price:0};

const reducer =(state = initialState,
    action)=>{

    switch (action.type) {
        case "UPDATE_ORDERS":
          
            break;
        default:
            return state
    }
}

export default reducer;